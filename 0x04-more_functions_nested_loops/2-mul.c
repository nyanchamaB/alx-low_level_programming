#include "main.h"
/**
 * mul - multiplies two integers 
 * @x: the first integer
 * @y: the second integer to be multiplied with first
 * Return: the multiplication result between x and y
 */

int mul(int a, int b)
{
	return (x * y);
}

#include "main.h"
/**
 * mul - multiplies two integers
 *
 * @x: the first integer
 * @y: the second integer to be multiplied x
 * Return: the multiplication result of x with y
 */

int mul(int x, int y)
{
	return (x * y);
}
